<html lang="<%= language %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= meta.title %></title>
  <meta name="description" content="<%= meta.description %>">
    <!-- Favicons -->
    <%- include('partials/head') %>

  <!-- Preload première image carousel (LCP) -->
  <% if (carouselProjects && carouselProjects.length > 0 && carouselProjects[0].image) { %>
    <link rel="preload" as="image" href="<%= carouselProjects[0].image %>">
  <% } %>

  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <!-- H1 SEO masqué visuellement -->
  <h1 class="visually-hidden"><%= t('home.title') %></h1>

  <!-- === NAVBAR === -->
  <header class="navbar">
    <span class="nav-left">
      <span class="first-name">Andrea</span><span class="last-name">Layton</span>
    </span>
    
    <!-- Navigation desktop -->
    <nav class="nav-links">
      <!-- Dropdown Projets -->
      <div class="dropdown">
        <button class="dropdown-btn nav-item"><%= t('nav.projects') %></button>
        <ul class="dropdown-menu">
          <% projects.forEach(project => { %>
            <li><a href="/<%= language %>/projets/<%= project.slug %>"><%= project.title %></a></li>
          <% }) %>
        </ul>
      </div>
      <a href="/<%= language %>/a-propos" class="nav-item"><%= t('nav.about') %></a>
      <a href="/<%= language %>/contact" class="nav-item"><%= t('nav.contact') %></a>
      <%- include('partials/language-switcher', { language, availableLanguages, req }) %>
    </nav>

    <!-- Navigation mobile -->
    <nav class="nav-links-mobile">
      <% projects.forEach(project => { %>
        <a href="/<%= language %>/projets/<%= project.slug %>" class="nav-item-mobile"><%= project.title %></a>
      <% }) %>
      <a href="/<%= language %>/a-propos" class="nav-item-mobile nav-item-mobile--highlight"><%= t('nav.about') %></a>
      <a href="/<%= language %>/contact" class="nav-item-mobile nav-item-mobile--highlight"><%= t('nav.contact') %></a>
      <div style="padding: 10px 0;">
        <%- include('partials/language-switcher', { language, availableLanguages, req }) %>
      </div>
    </nav>

    <!-- Menu burger -->
    <button class="burger" aria-label="Menu">
      <span></span>
      <span></span>
    </button>
  </header>

  <main>
    <!-- Données pour le carousel -->
    <div id="carousel-data" data-projects='<%= JSON.stringify(carouselProjects) %>'></div>
    
    <!-- Carousel Desktop/Tablette -->
    <section class="carousel-desktop">
      <div class="carousel">
        <!-- Les slides seront générées par JavaScript -->
        <button class="carousel__btn carousel__btn--prev" aria-label="Slide précédent"></button>
        <button class="carousel__btn carousel__btn--next" aria-label="Slide suivant"></button>
        <div class="carousel__indicators"></div>
      </div>
    </section>

    <!-- Liste mobile vertical -->
    <section class="projects-mobile">
      <% carouselProjects.forEach((project, index) => { %>
        <article class="project-card" id="project-<%= index %>">
          <a href="/<%= language %>/projets/<%= project.slug %>" class="project-card__link">
            <div class="project-card__image">
              <img src="<%= project.image %>" alt="<%= project.title %>" width="800" height="600" loading="lazy">
            </div>
            <div class="project-card__content">
              <h3 class="project-card__title"><%= project.title %></h3>
              <p class="project-card__description"><%= project.short_description %></p>
            </div>
          </a>
        </article>
      <% }) %>
    </section>
  </main>

  <script src="/js/carousel.js"></script>
</body>
</html>